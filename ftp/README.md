```
Directory struture:
.
├── data                         the directory used to store user uploaded files
│   └── README.md
├── docker-compose.yml           docker-compose file
├── README.md                    this file
└── scripts
    ├── add-user.sh              script to create ftp user
    ├── change-password.sh       script to change ftp users' password
    ├── start-server.sh          script to start the ftpd service
    └── stop-server.sh           script to stop the ftpd service
```
